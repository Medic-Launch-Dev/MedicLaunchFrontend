import { observer } from "mobx-react-lite";

function WhiteboardMedicine() {
  return (
    <div>
      <h1>Whiteboard Medicine</h1>
      <p>This is the Whiteboard Medicine page.</p>
    </div>
  );
}

export default observer(WhiteboardMedicine);